=============
API Reference
=============

The public API lives under ``src/`` and is grouped into focused sub-packages. Each page
opens with an overview table and then documents every public module in that area, with
NumPy-style ``Parameters`` / ``Returns`` sections and source links.

.. toctree::
   :maxdepth: 2

   config
   data
   models
   classical
   training
   evaluation
   inference
   utils
